# WhereIsMyMoney v0.1.1

金额显示修复版。

## 修复

- **金额显示出现两个小数点**（如 `1.500.00`）：千分位分隔符在部分环境下被 locale 渲染为 `.`。现改为手动格式化，所有金额固定为**逗号千分位 + 两位小数**（如 `1,500.00`、`8,552.30`）
- 编辑账目/预算时金额回填不再带千分位逗号，避免卡在输入校验上

## 测试

- 新增 9 项金额格式断言；应用内端到端自测 48 项全部通过（Debug / Release / 独立部署包三环境验证）
